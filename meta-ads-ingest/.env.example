META_ACCESS_TOKEN=your_long_lived_access_token
META_AD_ACCOUNT_ID=act_1813825642661559

# Telemetry seeder (optional overrides)
# META_API_VERSION=v25.0
# TARGET_CALLS_PER_RUN=25
# PAGE_LIMIT=25
# MAX_PAGES_PER_EDGE=5
# MIN_DELAY_MS=1200

# When running locally via node:
DATABASE_URL=postgresql://meta_user:meta_password@localhost:5432/meta_ads

# (When running via docker-compose, the worker uses postgresql://meta_user:meta_password@postgres:5432/meta_ads)
